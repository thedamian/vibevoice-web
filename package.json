{
  "name": "vibevoice-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"vite\" \"node --env-file=.env server/server.js\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node --env-file=.env server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^8.2.2",
    "vite": "^5.0.8"
  }
}
